# Safe RL with Control Barrier Functions

Training an RL agent on a constrained control task, then wrapping it in a control-barrier-function (CBF) safety filter to eliminate constraint violations without sacrificing task performance.

## Status
🚧 Week 0 — project setup in progress.

## Setup
Requires safe-control-gym installed separately (not on PyPI):
https://github.com/utiasDSL/safe-control-gym

pip install -e ".[dev]"